server:
  port: 8080
  servlet:
    context-path: /producto-api

spring:
  application:
    name: producto-api

  datasource:
    url: jdbc:postgresql://producto-db.cxkomie8sm3j.us-east-2.rds.amazonaws.com:5432/postgres
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
    database: postgresql

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8081/realms/master
  oAuthFlow:
    tokenUrl: http://localhost:8081/realms/master/protocol/openid-connect/token

post:
  baseUrl: https://jsonplaceholder.typicode.com/posts